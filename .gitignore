# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
.next/
out/
tsconfig.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Phase 0 baselines (large reports stay local; .baseline-build-from-ci.log is the only one committed)
.baseline-deps.txt
.baseline-size.txt
.baseline-lighthouse.report.html
.baseline-lighthouse.report.json
!.baseline-build-from-ci.log

# Lighthouse CI artifacts
.lighthouseci/

# Lighthouse manual reports per phase (no commitear, solo locales)
.phase*-lh*.json
.phase*-lh*.html

# graphify knowledge graph artifacts (locales, regenerables con /graphify)
graphify-out/

# Claude Code per-project state
.claude/
.vercel
.env*.local
